-- basically sisyphus table simulator
--[[
	TODO/ideas:
		* load sisyphus format files
--]]

grid=float_grid(w,h)

function sand_sim(  )
	--simulate sand piles tumbling down
end
function move( s,e )
	--move sphere from s to e pushing stuff around

	--calculate overlap
	--push overlap in direction of movement
	-- either push directly
	-- or push up then forward
end